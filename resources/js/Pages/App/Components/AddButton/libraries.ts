import { DefaultTheme } from 'styled-components';

export const getAddBtnTheme = (theme: DefaultTheme) => {
    return theme.tools.addBtn;
};

export const getAddBtnMeasures = (theme: DefaultTheme) => {
    return theme.measures.tools.addBtn;
};
