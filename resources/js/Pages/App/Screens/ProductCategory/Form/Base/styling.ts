import { InputForm_ } from '@/Pages/App/Components/InputForm';
import { styled } from 'styled-components';

export const ProdCategoryInput_ = styled(InputForm_)`
    &&:focus {
        box-shadow: none;
    }
`;
