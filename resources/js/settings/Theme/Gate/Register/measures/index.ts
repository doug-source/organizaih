import { RegisterThemeMeasures } from '@/settings/Theme/Gate/Register/measures/theme';

export const measures: RegisterThemeMeasures = {
    heading: {
        padding: {
            top: 8,
            bottom: 8,
        },
    },
};
