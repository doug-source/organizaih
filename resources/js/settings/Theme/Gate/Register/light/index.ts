import { measures } from '@/settings/Theme/Gate/Register/measures';
import { RegisterTheme } from '@/settings/Theme/Gate/Register/theme';

export const LightTheme: RegisterTheme = {
    register: {
        measures,
    },
};
