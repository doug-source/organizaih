import { body as loginBody } from '@/settings/Theme/Gate/Login/light/body';
import { RegisterTheme } from '@/settings/Theme/Gate/Register/theme';

export const body: RegisterTheme['register']['body'] = loginBody;
