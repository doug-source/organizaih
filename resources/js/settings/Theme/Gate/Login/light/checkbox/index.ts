import { blue, boxShadow, gray } from '@/settings/palette';
import { LoginTheme } from '../..';

export const checkbox: LoginTheme['login']['checkbox'] = {
    border: {
        color: gray[36],
    },
    boxShadow: boxShadow[40],
    color: blue[3],
};
