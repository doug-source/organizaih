import { gray } from '@/settings/palette';
import { LoginTheme } from '../..';

export const forgotPassword: LoginTheme['login']['forgotPassword'] = {
    color: 'inherit',
    hover: {
        color: gray[4],
    },
};
