import { measures } from '@/settings/Theme/Gate/RegisterRequest/measures';
import { RegisterRequestTheme } from '@/settings/Theme/Gate/RegisterRequest/theme';

export const LightTheme: RegisterRequestTheme = {
    registerRequest: {
        measures,
    },
};
