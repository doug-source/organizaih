import { DarkTheme } from '@/settings/Theme/Gate/ResetPassword/dark';
import { LightTheme } from '@/settings/Theme/Gate/ResetPassword/light';

export * from '@/settings/Theme/Gate/ResetPassword/theme';

export const ThemeData = {
    dark: DarkTheme,
    light: LightTheme,
};
