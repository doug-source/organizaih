import { measures } from '@/settings/Theme/Gate/ResetPassword/measures';
import { ResetPasswordTheme } from '@/settings/Theme/Gate/ResetPassword/theme';

export const DarkTheme: ResetPasswordTheme = {
    resetPass: {
        measures,
    },
};
