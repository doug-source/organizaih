import { DarkTheme } from '@/settings/Theme/Gate/ForgotPassword/dark';
import { LightTheme } from '@/settings/Theme/Gate/ForgotPassword/light';

export * from '@/settings/Theme/Gate/ForgotPassword/theme';

export const ThemeData = {
    dark: DarkTheme,
    light: LightTheme,
};
