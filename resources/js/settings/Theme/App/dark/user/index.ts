import { DefaultTheme } from 'styled-components';

export const user: DefaultTheme['user'] = {
    list: {
        tools: {
            nextBtn: {
                svg: {
                    fill: 'currentColor',
                },
                color: 'inherit',
            },
        },
    },
};
