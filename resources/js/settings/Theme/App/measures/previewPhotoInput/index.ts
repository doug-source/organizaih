import { DefaultTheme } from 'styled-components';

type PreviewPhotoInput = DefaultTheme['measures']['previewPhotoInput'];

export const previewPhotoInput: PreviewPhotoInput = {
    marginRight: 16,
    svg: {
        width: 64,
        height: 67.65,
    },
};
