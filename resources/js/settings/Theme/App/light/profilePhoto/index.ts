import { DefaultTheme } from 'styled-components';

export const profilePhoto: DefaultTheme['profilePhoto'] = {
    previewPhoto: {
        personCircle: {
            fill: 'currentColor',
        },
    },
};
